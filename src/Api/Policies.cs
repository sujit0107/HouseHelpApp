namespace HouseHelp.Api;

public static class Policies
{
    public const string RequireResident = nameof(RequireResident);
    public const string RequireHelper = nameof(RequireHelper);
    public const string RequireAdmin = nameof(RequireAdmin);
}
